version: '3.8'

services:
  web:
    build: .
    ports:
      - "4000:80"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: sh -c "npx tsc main.ts && http-server -p 80"